import { Text, View } from "react-native";

const Favorites = () => {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-3xl text-red-400">Favorites</Text>
    </View>
  );
};

export default Favorites;
